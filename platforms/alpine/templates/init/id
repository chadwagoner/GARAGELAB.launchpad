#!/sbin/openrc-run

name="id"
description="container - id"

restart_command="/usr/bin/docker compose -f /opt/core/id/compose.yaml restart"
start_command="/usr/bin/docker compose -f /opt/core/id/compose.yaml up -d"
stop_command="/usr/bin/docker compose -f /opt/core/id/compose.yaml down"
stop_command_args="--rmi all --volumes"

depend() {
        need docktail
        use docker tailscale
}

start() {
        ebegin "STARTING - $name"
        sleep 15
        ${start_command}
        eend $?
}

stop() {
        ebegin "STOPPING - $name"
        ${stop_command} ${stop_command_args}
        eend $?
}

restart() {
        ebegin "RESTARTING - $name"
        ${restart_command}
        eend $?
}
